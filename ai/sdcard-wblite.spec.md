# Task: implement a sdcard module for wblite

## Subtasks

### Subtask 1: implementation

* Reread `//GEMINI.md` for general advice.
* implement a wblite module at `//ip/wb` that interfaces to a SD card and supports
  reads and writes to the SD card.
* Use VUnit tests to verify the integration with wblite, using test modules
  from `//testing`, and the simulation model for SD card available at
  `//third_party/sdcard`.

